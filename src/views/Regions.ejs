<!DOCTYPE html>
<html lang="es">
<%- include('partials/head') %>

    <body>
        <%- include('partials/header') %>
            <main class="main--index">
                    <div class="div--regions-h1">
                        <h3>Litoral</h3>
                    </div>
                <section class="contenedor--tarjetas-index">
                    <% Litoral.forEach(({title, description, id, images,category, price, discount})=> { %>

                        <article class="tarjeta--individual-index">
                            <div class="img--trajeta">
                                <img class="img--trajeta" src="/images/productos/<%= images.find(image => image.main).file || 'producto-sin-imagen.png' %>" alt="">
                            </div>
                            <h4 class="titulo--tarjeta"> <%= title %> </h4>
                            <div class="index--price-discount__box">
                                <p class="precio--tarjeta"> <%= toThousand((price - (price * discount / 100)).toFixed(0))  %> </p>
                                 <% if (discount) { %>
                                    <p class="index--space">/</p>
                                    <p class="index--discount__p">% <%= discount %> OFF</p>
                                <% } %>
                            </div>
                            <button class="boton--tarjeta"><a href="/products/detail/<%= id %>">VER MAS</a></button>
                        </article>   

                        <% }) %>
                </section>
                
                <div class="div--regions-h1">
                    <h3>Centro</h3>
                </div>
                <section class="contenedor--tarjetas-index">

                    

                    <% Centro.forEach(({title, description, id, images,category, price, discount})=> { %>
                       
                         
                       
                        <article class="tarjeta--individual-index">
                            <div class="img--trajeta">
                                <img class="img--trajeta" src="/images/productos/<%= images.find(image => image.main).file || 'producto-sin-imagen.png' %>" alt="">
                            </div>
                            <h4 class="titulo--tarjeta"> <%= title %> </h4>
                            <div class="index--price-discount__box">
                                <p class="precio--tarjeta"> <%= toThousand((price - (price * discount / 100)).toFixed(0))  %> </p>
                                 <% if (discount) { %>
                                    <p class="index--space">/</p>
                                    <p class="index--discount__p">% <%= discount %> OFF</p>
                                <% } %>
                            </div>
                            <button class="boton--tarjeta"><a href="/products/detail/<%= id %>">VER MAS</a></button>
                        </article>   
                        
                        <% }) %>
                           

                </section>
                <div class="div--regions-h1">
                    <h3>Nuevo Cuyo</h3>
                </div>
                <section class="contenedor--tarjetas-index">
                    

                <% NuevoCuyo.forEach(({title, description, id, images,category, price, discount})=> { %>
                       
                         
                       
                        <article class="tarjeta--individual-index">
                            <div class="img--trajeta">
                                <img class="img--trajeta" src="/images/productos/<%= images.find(image => image.main).file || 'producto-sin-imagen.png' %>" alt="">
                            </div>
                            <h4 class="titulo--tarjeta"> <%= title %> </h4>
                            <div class="index--price-discount__box">
                                <p class="precio--tarjeta"> <%= toThousand((price - (price * discount / 100)).toFixed(0))  %> </p>
                                 <% if (discount) { %>
                                    <p class="index--space">/</p>
                                    <p class="index--discount__p">% <%= discount %> OFF</p>
                                <% } %>
                            </div>
                            <button class="boton--tarjeta"><a href="/products/detail/<%= id %>">VER MAS</a></button>
                        </article>   
                        
                        <% }) %>

                </section>

                <div class="div--regions-h1">
                    <h3>Costa Atlantica</h3>
                </div>
                <section class="contenedor--tarjetas-index">

                    

                    <% CostaAtlantica.forEach(({title, description, id, images,category, price, discount})=> { %>
                       
                         
                       
                        <article class="tarjeta--individual-index">
                            <div class="img--trajeta">
                                <img class="img--trajeta" src="/images/productos/<%= images.find(image => image.main).file || 'producto-sin-imagen.png' %>" alt="">
                            </div>
                            <h4 class="titulo--tarjeta"> <%= title %> </h4>
                            <div class="index--price-discount__box">
                                <p class="precio--tarjeta"> <%= toThousand((price - (price * discount / 100)).toFixed(0))  %> </p>
                                 <% if (discount) { %>
                                    <p class="index--space">/</p>
                                    <p class="index--discount__p">% <%= discount %> OFF</p>
                                <% } %>
                            </div>
                            <button class="boton--tarjeta"><a href="/products/detail/<%= id %>">VER MAS</a></button>
                        </article>   
                        
                        <% }) %>
                           

                </section>
            </main>
            <%- include('partials/scripts') %>
            <%- include('partials/footer') %>           
    </body>
</html>